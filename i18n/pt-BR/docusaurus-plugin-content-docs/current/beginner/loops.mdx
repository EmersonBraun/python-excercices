---
title: Loops & Iteração
sidebar_label: Loops & Iteração
sidebar_position: 4
---

# Loops & Iteração

Loops permitem repetir um bloco de código sem copiá-lo e colá-lo. Neste módulo você vai aprender os dois construtores de loop do Python — `for` e `while` — juntamente com as ferramentas que tornam a iteração mais expressiva: `range()`, `enumerate()`, `break`, `continue` e loops aninhados.

## Objetivos de Aprendizagem

- Escrever loops `for` sobre sequências e objetos `range()`
- Escrever loops `while` com condições de término corretas
- Usar `break` para sair de um loop antecipadamente e `continue` para pular uma iteração
- Usar `enumerate()` para iterar com um índice
- Construir loops aninhados para problemas multidimensionais

---

## Teoria

### Loops for

Um loop `for` itera sobre qualquer **iterável** — uma lista, string, tupla, range, etc.:

```python
fruits = ["apple", "banana", "cherry"]
for fruit in fruits:
    print(fruit)

for char in "Python":
    print(char)
```

:::tip
A variável do loop (`fruit`, `char`) é apenas uma variável comum. Você pode chamá-la de qualquer coisa, mas escolha um nome significativo.
:::

### range()

`range()` gera uma sequência de inteiros sem armazená-los todos na memória:

```python
range(5)          # 0, 1, 2, 3, 4
range(1, 6)       # 1, 2, 3, 4, 5
range(0, 10, 2)   # 0, 2, 4, 6, 8  (passo de 2)
range(10, 0, -1)  # 10, 9, ..., 1  (contagem regressiva)
```

Consulte [range()](https://docs.python.org/3/library/stdtypes.html#ranges) na documentação.

### Loops while

Um loop `while` se repete enquanto sua condição for `True`:

```python
count = 0
while count < 5:
    print(count)
    count += 1
```

:::note
Certifique-se sempre de que a condição do loop eventualmente se tornará `False`, caso contrário você criará um **loop infinito**.
:::

### break e continue

- `break` — sai imediatamente do loop envolvente
- `continue` — pula o restante da iteração atual e vai para a próxima

```python
for n in range(10):
    if n == 5:
        break       # para quando n chega a 5
    if n % 2 == 0:
        continue    # pula números pares
    print(n)        # imprime 1, 3
```

Consulte [break e continue](https://docs.python.org/3/tutorial/controlflow.html#break-and-continue-statements) no tutorial.

### enumerate()

Quando você precisa do índice e do valor durante um loop, use `enumerate()`:

```python
colors = ["red", "green", "blue"]
for i, color in enumerate(colors):
    print(f"{i}: {color}")
# 0: red
# 1: green
# 2: blue

# Começar a contar do 1:
for i, color in enumerate(colors, start=1):
    print(f"{i}. {color}")
```

### Loops Aninhados

Você pode colocar um loop dentro de outro. O loop interno completa todas as suas iterações para cada iteração do loop externo:

```python
for row in range(3):
    for col in range(3):
        print(f"({row},{col})", end=" ")
    print()   # nova linha após cada linha
```

:::tip
Cuidado com o desempenho: se ambos os loops executam `n` vezes, o corpo é executado `n²` vezes.
:::

Consulte o [Tutorial Python — Instruções for](https://docs.python.org/3/tutorial/controlflow.html#for-statements) para mais informações.

---

## Demonstração Visual

<div style={{width: '100%', maxWidth: '800px', margin: '0 auto'}}>
  <iframe
    src="/python-from-zero-to-hero/demos/bubble-sort/"
    width="100%"
    height="500"
    style={{border: '1px solid #e2e8f0', borderRadius: '8px'}}
    title="Bubble Sort Visualization"
  />
</div>

---

## Exercícios

:::info Como usar estes exercícios
Edite o código em cada painel e clique em **Run** (ou pressione Ctrl+Enter / Cmd+Enter) para executá-lo. Se casos de teste estiverem definidos, os resultados aparecem abaixo da saída.
:::

### Exercício 1 — Soma com for

<PythonRunner
  title="Sum with for"
  difficulty="easy"
  initialCode={`# Use a for loop to compute the sum of all integers from 1 to 10 (inclusive).
# Store the result in total.

total = 0
# Your loop here

print(total)
`}
  testCases={[
    { input: "total", expected: "55", description: "Sum of 1..10 should be 55" },
  ]}
/>

### Exercício 2 — Contagem Regressiva com range()

<PythonRunner
  title="range() Countdown"
  difficulty="easy"
  initialCode={`# Use range() to count DOWN from 5 to 1 and collect the numbers in a list.
# countdown should be [5, 4, 3, 2, 1]

countdown = []
# Your loop here

print(countdown)
`}
  testCases={[
    { input: "countdown", expected: "[5, 4, 3, 2, 1]", description: "countdown should be [5, 4, 3, 2, 1]" },
  ]}
/>

### Exercício 3 — Loop while

<PythonRunner
  title="while Loop"
  difficulty="easy"
  initialCode={`# Use a while loop to repeatedly halve a number until it falls below 1.
# Start with n = 64. Each iteration: append n to the list steps, then halve it.
# Stop when n < 1.

n = 64
steps = []
# Your while loop here

print(steps)
`}
  testCases={[
    { input: "steps", expected: "[64, 32.0, 16.0, 8.0, 4.0, 2.0, 1.0]", description: "steps should be [64, 32.0, 16.0, 8.0, 4.0, 2.0, 1.0]" },
  ]}
/>

### Exercício 4 — enumerate()

<PythonRunner
  title="enumerate()"
  difficulty="easy"
  initialCode={`# Use enumerate() to build a numbered list of items.
# For each item in menu, create a string "{i}. {item}" (1-based index)
# and append it to numbered_menu.

menu = ["Burger", "Pizza", "Salad", "Pasta"]
numbered_menu = []
# Your loop here

print(numbered_menu)
`}
  testCases={[
    { input: "numbered_menu[0]", expected: "1. Burger", description: "First item should be '1. Burger'" },
    { input: "numbered_menu[3]", expected: "4. Pasta", description: "Last item should be '4. Pasta'" },
    { input: "len(numbered_menu)", expected: "4", description: "Should have 4 entries" },
  ]}
/>

### Exercício 5 — break e continue

<PythonRunner
  title="break and continue"
  difficulty="medium"
  initialCode={`# Given the list of numbers below:
numbers = [3, 7, 2, 9, 4, 11, 6, 8, 1, 5]

# 1. Use a loop with 'continue' to build odds — a list of only odd numbers.
# 2. Use a loop with 'break' to find first_over_8 — the first number > 8.
#    Set first_over_8 to None if no such number exists.

odds = []
first_over_8 = None

# Your code here

print("Odds:", odds)
print("First over 8:", first_over_8)
`}
  testCases={[
    { input: "odds", expected: "[3, 7, 9, 11, 1, 5]", description: "odds should be [3, 7, 9, 11, 1, 5]" },
    { input: "first_over_8", expected: "9", description: "First number > 8 in the list is 9" },
  ]}
/>

### Exercício 6 — Tabela de Multiplicação

<PythonRunner
  title="Multiplication Table"
  difficulty="medium"
  initialCode={`# Build a 3x3 multiplication table using nested loops.
# table should be a list of lists where table[i][j] = (i+1) * (j+1)
# Example: table[0] = [1, 2, 3], table[1] = [2, 4, 6], table[2] = [3, 6, 9]

table = []
# Your nested loop here

for row in table:
    print(row)
`}
  testCases={[
    { input: "table[0]", expected: "[1, 2, 3]", description: "First row should be [1, 2, 3]" },
    { input: "table[1]", expected: "[2, 4, 6]", description: "Second row should be [2, 4, 6]" },
    { input: "table[2]", expected: "[3, 6, 9]", description: "Third row should be [3, 6, 9]" },
  ]}
/>

### Exercício 7 — FizzBuzz

<PythonRunner
  title="FizzBuzz"
  difficulty="medium"
  initialCode={`# Classic FizzBuzz: iterate over 1 to 20 (inclusive).
# Append to results:
#   "FizzBuzz" if divisible by both 3 and 5
#   "Fizz"     if divisible by 3 only
#   "Buzz"     if divisible by 5 only
#   the number  otherwise (as an int)

results = []
# Your loop here

print(results)
`}
  testCases={[
    { input: "results[0]", expected: "1", description: "1 is just 1" },
    { input: "results[2]", expected: "Fizz", description: "3 is Fizz" },
    { input: "results[4]", expected: "Buzz", description: "5 is Buzz" },
    { input: "results[14]", expected: "FizzBuzz", description: "15 is FizzBuzz" },
    { input: "len(results)", expected: "20", description: "Should have 20 entries" },
  ]}
/>

### Exercício 8 — find_primes (Bônus)

:::caution Desafio Antecipado
Este exercício usa loops aninhados e condicionais juntos. Está incluído como um **desafio bônus** para quem quiser avançar mais.
:::

<PythonRunner
  title="find_primes"
  difficulty="hard"
  initialCode={`# Write find_primes(n) that returns a list of all prime numbers up to n (inclusive).
# A prime number is a number greater than 1 with no divisors other than 1 and itself.
#
# Example: find_primes(20) -> [2, 3, 5, 7, 11, 13, 17, 19]

def find_primes(n):
    pass

print(find_primes(20))
print(find_primes(2))
`}
  testCases={[
    { input: "find_primes(20)", expected: "[2, 3, 5, 7, 11, 13, 17, 19]", description: "Primes up to 20" },
    { input: "find_primes(2)", expected: "[2]", description: "find_primes(2) should return [2]" },
    { input: "find_primes(1)", expected: "[]", description: "find_primes(1) should return []" },
    { input: "len(find_primes(50))", expected: "15", description: "There are 15 primes up to 50" },
  ]}
/>
